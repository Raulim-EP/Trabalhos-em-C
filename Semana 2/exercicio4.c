// receba uma palavra e informe o numero de vogais digitadas;
#include <stdio.h>
#include <string.h>
char palavra[10];
int n_word;

int main(){

    printf("Insira uma palavra: ");
    gets(palavra);
    n_word = strlen(palavra);
    for(int i = 0; i < n_word; i++){
        

    }
    return 0;
}